import AuditingIcon from '../../../icons/audit/en_US/auditing';
import CompleteIcon from '../../../icons/audit/en_US/complete';
import ExpiredIcon from '../../../icons/audit/en_US/expired';
import RejectIcon from '../../../icons/audit/en_US/reject';
import CancelIcon from '../../../icons/audit/en_US/cancel';
import RevokedIcon from '../../../icons/audit/en_US/revoked';

export default {
  lang: {
    sealIcon: {
      auditing: AuditingIcon,
      complete: CompleteIcon,
      pending: CompleteIcon,
      pass: CompleteIcon,
      expired: ExpiredIcon,
      reject: RejectIcon,
      cancel: CancelIcon,
      revoked: RevokedIcon,
    }
  }
};